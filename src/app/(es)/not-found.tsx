import { NoEncontrada } from "@/components/layout/NoEncontrada";

/** 404 de los `notFound()` que lanzan las páginas en castellano. */
export default function NotFound() {
  return <NoEncontrada />;
}
